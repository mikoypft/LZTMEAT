# Frontend Environment Configuration

# Set which backend to use: 'laravel' or 'supabase'
VITE_API_MODE=laravel

# Laravel backend URL (used when VITE_API_MODE=laravel)
VITE_API_BASE=http://localhost:8000/api

# If using Supabase backend, set VITE_API_MODE=supabase
# Then the system will use the Supabase credentials from utils/supabase/info.tsx
